AWS_REGION=us-east-1
EC2_INSTANCE_TYPE=t3.large        # 2 vCPU, 8GB RAM (needed for Chrome)
EC2_AMI=                           # Ubuntu 24.04 LTS (auto-detected if blank)
EC2_KEY_NAME=job-apply-key
EC2_VOLUME_SIZE=30                 # GB
MY_IP=                             # Auto-detected via checkip.amazonaws.com if blank
ANTHROPIC_API_KEY=
TWOCAPTCHA_API_KEY=
OPENCLAW_GATEWAY_TOKEN=            # Random 64-char token (auto-generated if blank)
WHATSAPP_ALLOW_FROM=               # Your phone number in E.164 format
